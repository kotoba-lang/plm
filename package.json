{
  "scripts": {
    "build": "amu compile --target wasm32-browser app",
    "watch": "amu compile --target wasm32-browser app"
  },
  "dependencies": {
    "shadow-cljs": "latest",
    "react": "latest",
    "react-dom": "latest"
  }
}
